# X5 Tech Internship Hiring — Proposal / Scope (Telegram-first автоматизация найма)

## 1) Вводная: что за задача к нам пришла

На хакатоне нам принесли очень практичную проблему: **процесс найма стажёров в X5 Tech сейчас перегружает рекрутеров и плохо масштабируется**.
Формально процесс работает, но “стоимость” каждого кандидата (время, коммуникации, ручной контроль) слишком высокая — из-за этого растёт срок закрытия вакансий и падает качество опыта кандидатов.

---

## 2) Как выглядит процесс сейчас (коротко, но по делу)

* Кандидаты подаются через сайт.
* Два раза в неделю база откликов выгружается в **Excel**.
* Рекрутер вручную:

  * фильтрует отклики по позициям,
  * смотрит, что прислали (часть откликов — мусор, мемы, “котики” вместо резюме),
  * связывается с кандидатами и проводит первичный скрининг,
  * дальше начинает “пинг-понг” с менеджером по интервью (время, слоты, переносы),
  * собирает фидбек, обновляет статусы вручную.

---

## 3) Ключевые проблемы, которые важно исправить

### 3.1. Скорость

**Time-to-fill растёт**, потому что очень много ручных шагов до того, как кандидат вообще попадает на интервью.

### 3.2. Прозрачность для кандидата

Кандидат не понимает, где он в процессе, и часто просто “ждёт в тишине”. Это портит опыт и повышает отвал сильных ребят.

### 3.3. Нагрузка на рекрутера (не там, где надо)

Рекрутер вынужден быть “оператором процесса”: статусы, напоминания, переносы, согласования, сбор фидбека.
Хотя в идеале он должен заниматься тем, где действительно нужен человек: сложные кейсы, исключения, конфликтные ситуации.

### 3.4. Мало структурированных данных

По стажёрам часто нет нормальных резюме, а данные приходят хаотично. Без структуры сложно:

* сравнивать кандидатов,
* оценивать воронку,
* понимать, где “просадка” и почему.

### 3.5. Отдельная важная боль: “фильтр адекватности”

На практике рекрутер часто выполняет роль **барьера от токсичных/неадекватных кандидатов**, которые не умеют разговаривать и превращают интервью в потерю времени.
Этот слой всё равно нужен — вопрос в том, как **снять с рекрутера максимум рутины**, а “фильтр адекватности” частично автоматизировать.

---

## 4) Наш подход: автоматизация без тяжёлого фронта

Команда небольшая, “фронта нет”, поэтому мы сознательно выбираем путь **Telegram-first**:

* быстро собрать MVP,
* минимизировать разработку интерфейсов,
* при этом дать кандидату понятный “личный кабинет” прямо в привычном канале.

В результате мы строим решение из **трёх модулей**, где каждый модуль закрывает конкретную часть боли.

---

# 5) Предлагаемое решение: 3 модуля

## Модуль A — Candidate Flow: лендинг + Telegram-бот кандидата

**Цель модуля:** сделать вход в воронку структурированным, убрать мусор, собрать данные, дать кандидату прозрачный процесс.

### A1) Одностраничный лендинг (минимальный, но аккуратный)

* описание треков стажировок (направлений)
* одна понятная кнопка “Податься”
* по клику кандидат сразу попадает в Telegram-бота (без лишних форм и регистраций)

### A2) Telegram-бот: сбор данных + первичный отбор

1. **Шаг “Есть резюме?”**

   * если есть — кандидат отправляет PDF
   * мы парсим резюме и предзаполняем поля (чтобы человек не переписывал всё руками)
   * параллельно делаем базовую валидацию, чтобы отсекать мусорные файлы

2. **Анкета “расскажи о себе” (важно для стажёров)**
   Стажёры часто не умеют писать резюме, поэтому анкета — это “структурированное резюме”, где мы собираем:

   * учебу (школа/вуз/курс)
   * достижения (олимпиады, проекты, хакатоны)
   * ссылки (GitHub/портфолио)
   * и дополнительно **доменные интересы/опыт**, чтобы лучше матчить под команды
     Примеры: робототехника, логистика/склады, ERP/SAP, аналитика, продуктовые домены.

3. **Выбор трека вместо “укажи стек”**
   Мы намеренно не заставляем стажёра писать “я знаю React/Go/что-то ещё”, потому что он часто не понимает, что корректно указать.
   Вместо этого кандидат выбирает **направление**, а дальше мы проверяем базу знаний трека через квиз.

4. **Quiz по треку (тайм-лимит, банк вопросов)**

   * вопросы разные для разных направлений
   * ограничение по времени снижает “гугление” и добавляет честности
   * результат квиза превращается в понятный скоринг для первичного отбора

5. **Прозрачные статусы и уведомления**
   В боте кандидат всегда видит, что происходит: “анкета принята”, “на проверке”, “интервью назначено”, “есть фидбек” и т.д.
   Это снижает нагрузку на рекрутера (“а что со мной?”) и повышает доверие к процессу.

### A3) Приглашение на интервью и самозапись в слоты (важный операционный кусок)

Когда кандидат проходит первичный фильтр и **конкретная команда готова его посмотреть**, бот отправляет человеку понятное “приглашение”:

* “Тебе заинтересовалась команда **X** по позиции **Y**”
* коротко: **чем занимается команда / какой продукт / почему это может быть тебе интересно**
* кнопки: **«Хочу пройти собеседование» / «Пока не актуально»**

Если кандидат нажимает **«Хочу пройти собеседование»**, бот показывает **доступные слоты** из календаря нанимающего менеджера (или пула интервьюеров), и кандидат просто выбирает удобное время.

Если подходящих слотов нет, кандидат нажимает кнопку:

* **«Нет подходящего времени»**
  И это автоматически превращается в **задачу рекрутеру** в модуле C (в канбан/бэклог), чтобы рекрутер подключился только там, где реально нужен человек: договориться, расширить окно, найти альтернативу, помочь с переносом.

### A4) Идея для усиления “фильтра адекватности” (как bullet / расширение)

На входе можно добавить короткий **видео-скрининг** в Telegram (кружочки): 2–3 вопроса, ответы по 20–30 секунд.
Это даёт нам простую возможность заранее увидеть:

* как человек говорит,
* насколько он адекватно формулирует мысли,
* базовую мотивацию/вменяемость.
  Даже если в MVP не успеем — это сильный пункт в концепции.

---

## Модуль B — Telegram-бот нанимающего менеджера (расширенная логика принятия решений)

**Цель модуля:** убрать хаос “кандидаты в чатах/почте”, ускорить решения менеджера и сделать фидбек структурированным (а значит — пригодным для дальнейших действий и аналитики).

Менеджер в боте работает не “текстом в пустоту”, а через быстрые управляемые действия.

### B1) Что видит менеджер по кандидату

Каждый кандидат приходит как **карточка**, где в одном месте:

* резюме (если было) + структурированная анкета
* результаты квиза (скоринг / какие темы просели)
* доменные теги (робототехника / SAP / логистика и т.п.)
* короткое “summary” (можно авто-сгенерить из анкеты/резюме)

### B2) “Что делаем дальше?” — кнопки вместо хаоса

После просмотра карточки менеджер выбирает один из понятных исходов (кнопками), чтобы система дальше сама вела кандидата:

1. **✅ Пригласить на интервью**

   * кандидат получает приглашение (см. A3) и выбирает слот сам
   * менеджеру не нужно переписываться и согласовывать вручную

2. **🟡 В ожидание (shortlist / на паузу)**

   * кандидат не “отваливается”, а попадает в список “посмотрю позже”
   * удобно, когда менеджер видит 10 кандидатов и хочет оставить 1–2 “в запас”

3. **🔁 Рекомендовать другой команде / другому треку**

   * менеджер отмечает: “мне не подходит, но кандидат сильный”
   * система может:

     * предложить релевантные вакансии/команды по тегам и профилю
     * или отправить рекрутеру задачу “перенаправить кандидата”

4. **❌ Отказать**

   * желательно с короткой причиной (из списка), чтобы:

     * кандидату можно было дать корректный статус/обратную связь
     * в аналитике копились реальные причины отказов

### B3) Быстрый фидбек после интервью (структурировано)

После интервью менеджер получает “форму-мини” (в Telegram):

* хард-скиллы (0–5)
* коммуникация / адекватность (0–5)
* мотивация (0–5)
* комментарий (опционально)
* итог: “берём / держим / не берём / перенаправить”

Это решает две задачи:

1. менеджеру проще дать фидбек (не надо писать простыню)
2. рекрутер и система понимают, что делать дальше **без ручной расшифровки**

### B4) Управление слотами

Менеджер задаёт доступность:

* “вот мои окна для интервью на этой неделе”
* можно быстро обновлять (добавить/убрать окна)
* слоты автоматически попадают кандидату в самозапись

---

## Модуль C — Recruiter Dashboard: контроль + исключения + задачи

**Цель модуля:** не делать “ещё один ATS”, а дать рекрутеру простой инструмент управления автоматизацией и ручными исключениями.

### C1) Дэшборд процессов

* сколько вакансий открыто
* сколько кандидатов на каждом этапе
* где “зависли” кандидаты и нарушены SLA

### C2) Канбан/бэклог задач рекрутера (самое важное)

Рекрутеру не нужно “следить за всем глазами”. Ему нужен список задач, где он реально требуется, например:

* кандидат нажал **«нет подходящего времени»** → нужно вручную договориться/расширить окна
* перенос интервью / конфликт времени / кандидат пропал
* нестандартный случай (сомнительный профиль, ручной адекватность-чек)
* запрос кандидата на уточнения, эскалация, исключение из правил

Задачи можно снабдить:

* приоритетом (high / medium / low)
* дедлайном (SLA)
* понятным статусом обработки

### C3) Данные и аналитика (минимально, но полезно)

* качество входящего потока (сколько валидных профилей)
* конверсии по этапам
* причины отказов (хотя бы на уровне шаблонов)
* time-in-stage (где “тормозит”)

---

# 6) Что именно автоматизируем “под капотом”

* единая база кандидатов (структурированные поля из анкеты + резюме + квиз)
* автоматические статусы и уведомления
* самосервис назначения интервью через слоты менеджера
* первичный скоринг/фильтрация (анкета + квиз + базовые проверки)
* рекомендательный матчинг по доменным тегам (роботы / SAP / логистика и т.п.)

---

# 7) MVP-границы (чтобы реально показать на хакатоне)

**MVP обязателен:**

* Candidate bot: резюме/анкета → выбор трека → квиз → статусы → приглашение → запись в слоты
* HM bot: кандидаты → кнопки “что дальше” → слоты → структурированный фидбек
* Recruiter dashboard (минимальный): список кандидатов + статусы + “очередь задач/исключений”

**В концепцию (как расширение):**

* видео-кружочки как “адекватность-скрининг”
* более умный матчинг по доменным тегам
* расширенная аналитика и SLA-отчёты
