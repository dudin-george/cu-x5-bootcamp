replicaCount: 1

config:
  environment: dev
  port: 8000
  frontendUrl: "http://localhost:5173"

secrets:
  databaseUrl: ""  # Set via --set

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

service:
  port: 8000
