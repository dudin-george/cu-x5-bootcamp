apiVersion: v1
kind: Service
metadata:
  name: {{ include "candidate-bot.fullname" . }}
  labels:
    {{- include "candidate-bot.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.config.port }}
  selector:
    {{- include "candidate-bot.selectorLabels" . | nindent 4 }}
